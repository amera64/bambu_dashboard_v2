#pragma once

#include <Arduino.h>

void drawNozzleIcon(int x, int y, uint16_t color);
void drawBedIcon(int x, int y, uint16_t color);
void drawPauseIcon(int x, int y, uint16_t color);
void drawPlayIcon(int x, int y, uint16_t color);
void drawStopIcon(int x, int y, uint16_t color);
void drawLightIcon(int x, int y, uint16_t color);
void drawLayerIcon(int x, int y, uint16_t color);
void drawClockIcon(int x, int y, uint16_t color);
void drawWiFiIcon(int x, int y, uint16_t color);
void drawChamberIcon(int x, int y, uint16_t color);
void drawHumidityIcon(int x, int y, uint16_t color);

void drawHomeIcon(
    int x,
    int y,
    uint16_t color
);

void drawAMSIcon(
    int x,
    int y,
    uint16_t color
);

void drawTemperatureIcon(
    int x,
    int y,
    uint16_t color
);

void drawControlIcon(
    int x,
    int y,
    uint16_t color
);

void drawSystemIcon(
    int x,
    int y,
    uint16_t color
);


#pragma once

#include <Arduino.h>



// Keep your other icon declarations below.